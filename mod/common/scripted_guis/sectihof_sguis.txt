sectihof_old_hof_title_name = {
	#scope = character
	scope = title

	effect = {
		save_scope_as = effect_root
		#debug_log_scopes = yes

		# primary_title = { # FIXME: Tmp
		# 	save_scope_as = old_hof_title
		# }

		custom_description_no_bullet = {
			text = sectihof_effect_old_hof_title
			#value = scope:old_hof_title
			#subject = scope:old_hof_title
			#object = scope:old_hof_title
			subject = root
			object = root
		}
	}
}
