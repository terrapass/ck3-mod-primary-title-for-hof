# This requires a new game :(
# Maybe I can use a title template somehow
# and create the actual title at runtime?
# (Though in that case flavorization would probably go out the window.)
d_sectihof_prefer_religious = {
	color = { 255 0 255 }

	definite_form = yes

	destroy_if_invalid_heir = yes
	no_automatic_claims = yes
	definite_form = yes
	always_follows_primary_heir = yes
}
